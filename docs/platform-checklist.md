# v3 平台与真机验收清单

## 使用规则

本清单用于验收 Three.js/WebGL2 单 Canvas 重构的最终构建产物。未勾选表示“尚无当前证据”，不能从自动化测试、桌面浏览器或开发者工具的结果推断为真机已通过。

每次勾选应同时记录：

- 构建的 commit 或产物标识。
- 平台和客户端/基础库版本。
- 设备型号、系统版本、屏幕方向和网络。
- 截图、屏幕录像、错误日志或性能采样的存放位置。
- 验收人与日期。

## 本次验收记录

| 字段 | 内容 |
|---|---|
| 构建/commit | `render-governance-b4` / `5a113d6`（渲染性能四批终验） |
| Web 预览地址 | `http://192.168.1.249:4173/`（2026-07-15 最新生产构建；HTTP 200） |
| 微信开发者工具 | 待填写 |
| 抖音开发者工具 | 待填写 |
| 微信 iOS 设备 | 待填写 |
| 微信 Android 设备 | 待填写 |
| 抖音 iOS 设备 | 待填写 |
| 抖音 Android 设备 | 待填写 |
| 证据目录 | [`docs/render-final-v3.jpg`](render-final-v3.jpg)、[`public/assets/concept/web-jump-three-v3.png`](../public/assets/concept/web-jump-three-v3.png) |
| 验收人/日期 | Codex 本机自动化与浏览器验收 / 2026-07-15；项目方确认 Web 手机验收完成 / 2026-07-15（设备信息未记录） |
| 本次 Web 真机目标 | iPhone 13 Pro / iOS 26 / Chrome；渲染性能第四批最终提交，待项目方验收。 |

## 自动化与产物前置检查

- [x] `npm run test` 的 25 个文件、158 项测试通过，覆盖 5 个正式玩法、5 个正式任务、10 个正式角色、高低画质、存档调度、数值、WorldState、跳跃/碰撞、平台隔离、1000 会话和 100 轮资源测试。
- [x] `npm run build` 生成 `dist/web`、`dist/wechat` 和 `dist/douyin`。
- [x] 三个分发目录都包含 `THIRD_PARTY_NOTICES.md` 和 `licenses/`。
- [x] 小游戏构建是可直接加载的 IIFE，不依赖 DOM、Node 或运行时模块加载器。
- [x] 平台 API 隔离测试证明 `wx.*` 和 `tt.*` 未泄漏到 Gameplay、Jump Engine、Application 或 Render3D。
- [x] 构建脚本枚举并强制 Web gzip 180 KiB、微信/抖音 `game.js` 700 KiB 的项目预算。
- [ ] 与发行平台账号当前包体限制核对。

当前本地构建：`dist/web` 约 `5.3 MiB`（含 source map），Web JS 684.91 kB、gzip 180,771 bytes；`dist/wechat` 与 `dist/douyin` 各约 `704 KiB`，其中 `game.js` 700,008 bytes。发行平台限制仍需在真实账号和开发者工具中核对。

## 共用 WebGL2 和视觉

- [x] 本机浏览器初始化只使用一个上屏 Canvas，World Scene 与 HUD Scene 都正常显示。
- [ ] 设备能创建 `webgl2` 上下文；不支持时显示可诊断信息，而不是黑屏。
- [x] 本机浏览器清屏背景为 `#D8DDE2`，角色红为 `#E53935`，选中青为 `#16A6A1`，主文字为 `#263238`。
- [x] 本机浏览器落地后主视觉是一个当前平台、两个左右候选和一个退后的历史平台。
- [x] 本机浏览器正交斜俯视下平台顶面、侧面、角色和候选运算都清晰可读。
- [x] 本机浏览器环境光、方向光和动态阴影无明显条带、闪烁、阴影 acne 或断层。
- [x] 构建与本机浏览器中未出现旧狐狸、数据城市远景/前景、节拍轨和常驻底部操作轨道。
- [x] 本机浏览器顶部极简 HUD 正确显示当前值、目标值和剩余步数，在所测视口中无裁切。
- [x] 单 Canvas 内容菜单在 `390×844` 和 `1280×800` 完整显示；玩法、兼容任务、10 个角色和高/低画质可循环选择并一起应用。
- [x] 本机浏览器选择候选时，平台和对应运算标签有单一、明确的青色反馈。

## 共用玩法与动作

- [x] 本机浏览器验证只有底部 `↖` / `↗` 按钮可分别锁定屏幕左/右候选；按钮高亮和实际飞行方向一致，点击场景不蓄力，一次操作只跟踪一个触点。
- [ ] 按住期间角色变矮变宽，当前平台同步受压；反馈连续且与按住时长一致。
- [ ] 松开后角色和平台回弹，角色沿 Core 抛物线起跳并完成局部前空翻。
- [ ] 角色根位置、地面阴影和拖尾方向与实际轨迹一致，没有视觉吸附。
- [ ] 成功落地只校正高度，角色在平台上的真实偏心 `x/z` 位置保留。
- [ ] 相同 seed 与相同蓄力时长在不同刷新率下获得相同碰撞结果。
- [ ] 成功落地后所选平台保持 ID 并晋升，未选分支退场，新左右候选生成。
- [x] 本机浏览器采样确认世界原点与镜头焦点经过 `80 ms` 停留和 `440 ms` 同步位移平滑前进；视觉平移不会改变下一跳距离。
- [ ] 成功落地后才执行运算与扣步数；短跳、越过和边缘失败不修改数值或步数。
- [ ] 短跳、越过和边缘失败有区分度，倾倒/滑落/坠落动画不把失败修正为成功。
- [ ] 暂停冻结输入提交、Core 更新和表现时间轴；恢复后不追赶大段后台时间。
- [ ] 重开、胜利下一局和失败重开都不重复绑定输入、帧循环或上下文事件。

## Web 浏览器

- [x] `dist/web` 可作为静态站点直接访问，且本机浏览器控制台无未处理错误和 WebGL 编译警告。
- [ ] 桌面鼠标、手机触摸和页面隐藏/恢复均正常。
- [x] 本机浏览器以 `390×844` 手机尺寸持续按住箭头时只驱动蓄力/起跳，未产生文本选区、上下文菜单或焦点异常。
- [x] 本机浏览器先左后右成功落地（18→23→32），刷新后从本地存档恢复为“当前 32 / 剩余 5”，且 console 记录为空。
- [ ] 手机浏览器长按箭头只触发蓄力，不出现复制菜单、文本选区、图片拖拽或页面手势抢占。
- [ ] 更换 Wi-Fi 后重新发现电脑局域网 IP，手机使用新 `Network` 地址访问成功。
- [x] 本机浏览器实测手机竖屏 `390×844`、概念原生长屏 `941×1672` 与桌面宽窗口 `1100×900`，HUD 和两个候选均可读。
- [ ] 上下文丢失测试能停止错误渲染；恢复后 Scene 从 Core 快照重建，数值与世界不倒退。

## 微信开发者工具

- [ ] `dist/wechat` 可导入，配置为竖屏并使用真实 AppID。
- [ ] 使用 `wx.createCanvas()` 创建上屏 Canvas，离屏资源不意外变成第二个上屏 Canvas。
- [ ] `webgl2` 上下文、首帧、阴影、HUD、字形和透明资源在工具中正常。
- [ ] `wx` 触摸开始、结束和取消事件能驱动左右选路和真实蓄力。
- [ ] 前后台、震动和本地存储只经由微信适配器。

## 微信真机

- [ ] iOS：WebGL2 创建、首帧、完整一局、暂停/恢复、进后台再回前台、重开、安全区均通过。
- [ ] Android：WebGL2 创建、首帧、完整一局、暂停/恢复、进后台再回前台、重开、安全区均通过。
- [ ] iOS 与 Android 都完成至少 10 分钟连续游玩，无持续内存增长、帧率崩落、明显发热异常或黑屏。
- [ ] 上下文恢复能力有真机证据；若平台不暴露对应事件，记录受限行为而不虚构通过。

## 抖音开发者工具

- [ ] `dist/douyin` 可导入，配置为竖屏并使用真实 AppID。
- [ ] 使用 `tt.createCanvas()` 创建上屏 Canvas，离屏资源不意外变成第二个上屏 Canvas。
- [ ] `webgl2` 上下文、首帧、阴影、HUD、字形和透明资源在工具中正常。
- [ ] `tt` 触摸开始、结束和取消事件能驱动左右选路和真实蓄力。
- [ ] 前后台、震动和本地存储只经由抖音适配器。

## 抖音真机

- [ ] iOS：WebGL2 创建、首帧、完整一局、暂停/恢复、进后台再回前台、重开、安全区均通过。
- [ ] Android：WebGL2 创建、首帧、完整一局、暂停/恢复、进后台再回前台、重开、安全区均通过。
- [ ] iOS 与 Android 都完成至少 10 分钟连续游玩，无持续内存增长、帧率崩落、明显发热异常或黑屏。
- [ ] 上下文恢复能力有真机证据；若平台不暴露对应事件，记录受限行为而不虚构通过。

## 性能和资源

- [x] 本机生产预览：低档 DPR 1.5、高档 DPR 1.75；刷新保持低档，切回高档恢复。
- [x] 本机高档松手响应 6.6ms、低档 5.7ms；各 240 帧窗口超过 50ms 长帧均为 0，起跳 CanvasTexture 新增为 0。
- [x] 本机 SaveScheduler queued/flushes 为 2/2、失败 0；自动化证明首个 Renderer 帧不写存储，Hide/Destroy 强制刷新。
- [x] 内容菜单保持单一可重绘动态纹理；关闭菜单只释放菜单纹理，常驻顶栏、状态条使用固定动态纹理，不再随数值增加缓存。
- [x] 当前生产构建高画质 5 次连续成功落地：P99 15.4ms、空中最大 17.6ms、落地最大 18.0ms、超过 20ms 为 0、空中重复率 0.25%。
- [x] 当前生产构建低画质 5 次连续成功落地：P99 10.3ms、空中/落地最大 10.4ms、超过 20ms 为 0、空中重复率 0%。
- [x] 两档压力验收 Runtime/Renderer/console error 为 0；详细证据见空中与落地卡顿修复验证。
- [ ] 记录待机、蓄力、空中、粒子爆发和连续 10 分钟后的 FPS、draw calls、三角形和内存。
- [ ] 目标中端设备常态 55–60 FPS；如果未达到，附采样证据和降级决策。
- [ ] 常态 draw calls 目标不超过 50，三角形目标不超过 30k，纹理显存目标低于 32MB；以采样而不是代码推断勾选。
- [ ] 多次重开、多局连续进行和切后台后，Mesh、Material、Texture、粒子和拖尾数量回到有界稳态。
- [ ] 阴影降级在低端设备上生效且不改变碰撞或 HUD 可读性。
- [ ] iPhone 13 Pro / iOS 26 / Chrome 分别使用高、低画质完成长按松手、连续跳跃、刷新恢复、后台返回和 10 分钟稳定性验收。

## 异常处理

- [ ] WebGL2 不可用：显示平台标识和可操作建议，保留诊断日志。
- [ ] 资源加载失败：不进入可交互状态，不留下无反馈黑屏。
- [ ] 触摸取消/来电/强制切后台：清理本次蓄力，不意外提交跳跃。
- [ ] 构建、导入或真机阻断时：保留原始错误和复现步骤，不将未验证项勾选为通过。

视觉准则见 [v3 视觉与动作系统](design-system-v3.md)，架构约束见 [v3 技术架构](architecture.md)。
