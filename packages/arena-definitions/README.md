# @number-strategy-jump/arena-definitions

Arena 权威层最底部的不可变 Definition 与只读 Registry。当前公开动作和角色合同；后续 Definition 必须保持纯数据、构造即校验、稳定排序和重复 ID 拒绝。

本包只允许依赖 `arena-contracts`，不得依赖 Rule、Core、Bot、Presentation、Three.js、DOM 或平台 API。
