# 四批治理独立终验

终验日期：2026-07-15

终验基线：`governance-b4` / `8633822b6abfd944efe4b9540087b4735f85bf77`

## 结论

第 0 批与 P0–P10 四批治理在仓库自动化、本机三端构建和 Web 生产预览范围内通过，未发现阻断性缺陷。P0–P8 冻结的数值、世界、碰撞、左右按钮和镜头过渡基线保持通过；第四批新增的版本化本地存档、全面 TypeScript 与工程门禁已成为累积约束。

微信/抖音开发者工具、iOS/Android 真机、低端设备长时性能和 GitHub 服务端分支保护不属于本机可伪造的证据，继续保留在发布清单中，不能从本结论推断为已经通过。

## 独立自动化结果

- `npm run check`：通过。
- strict TypeScript、ESLint、零旧 JS、依赖方向、文档链接：通过。
- Vitest：23 个文件、138 项测试通过。
- 可解性：easy/normal/hard 各 10,000 seed，共 30,000 回合通过。
- 会话 soak：1,000 个完整 normal 会话获胜且剩余步数非负。
- 资源 soak：100 局 Three 平台视图保持有界，结束后活动视图与 Object3D 子节点归零。
- 覆盖率：确定性单测层行/语句 89.42%、函数 90.83%、分支 70.17%。
- 扩展容量：5 Gameplay、5 Task、10 Character Manifest 的注册、选择、回退/销毁证明通过。
- 存档：v1/v2/v3 fixture、迁移回写、损坏清除、动作回放、首帧恢复和诊断导出通过。
- 安全与许可：生产依赖 0 个已知漏洞；1 个第三方运行时依赖完成白名单与许可归属；内置角色无外链资产。
- 构建：Web、微信、抖音通过；Web gzip 170,812 bytes，小程序 `game.js` 665,797 bytes，均低于项目硬预算。

## 独立 Web 生产验收

地址：`http://192.168.1.249:4173/`（只对当前 Wi-Fi 有效）

1. 390×844 首屏从第四批浏览器存档恢复为“当前 32 / 目标 57 / 剩余 5”。
2. 执行 `frozen → active` 生命周期切换后，帧循环与画面保持可用。
3. 长按屏幕左按钮 650ms，成功落到左侧 `×2` 路径，状态变为“当前 64 / 剩余 4”。
4. 刷新页面，首帧恢复为“当前 64 / 剩余 4”，世界与玩家落点一致。
5. DOM 检查为 1 个 Canvas，body/Canvas `user-select:none`，文本选区为空。
6. 浏览器 console 日志为空，无 error/warn。

第四批此前还独立完成了左按钮 18→23、右按钮 23→32，并在刷新后恢复 32/5，证明两侧路径和多动作回放顺序均有效。

## 发布前仍需人工完成

- 在当前手机浏览器打开上述地址，完成至少一次左/右长按、刷新恢复、声音与振动开关体验；更换 Wi-Fi 后重新发现 IP。
- 将 `dist/wechat`、`dist/douyin` 导入对应开发者工具，填写真实 AppID，并完成 iOS/Android 真机矩阵。
- 记录至少 10 分钟低端真机 FPS、内存、发热、draw calls 和 context lost/restored 行为。
- 仓库管理员在 GitHub main 启用 required `quality`、禁止直接推送和至少 1 名 CODEOWNER 审批。

详细步骤见 [发布清单](../release-checklist.md)和[平台真机验收清单](../platform-checklist.md)。
