# Arena Storage

跨 Product、Study 与 Pilot 复用的同步存储所有权协议。该包只负责宿主同步存储上的租约竞争、续租、确认释放与旧 schema 兼容；不拥有任何业务聚合、存档 schema 或迁移规则。

- 构造参数与存储值只接受可枚举数据字段，不执行访问器。
- 宿主端口在构造期快照，所有外部回调期间拒绝重入。
- 写入、删除异常以后续权威读回为准；无法确认的状态失败关闭。
- 销毁只有在确认释放后才完成，失败时保留状态供调用方重试。
