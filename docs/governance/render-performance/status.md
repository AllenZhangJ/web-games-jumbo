# 渲染性能治理状态

更新时间：2026-07-15

当前分支：`feature/render-performance-governance`

| 批次 | 状态 | 当前事实 |
|---|---|---|
| 第一批：基线、契约与守卫 | 已完成 | 提交 `0a0af93`、标签 `render-governance-b1` 与远端分支一致。 |
| 第二批：资源内核与 HUD | 已完成 | 提交 `15a7ed7`、标签 `render-governance-b2` 与远端分支一致。 |
| 第三批：帧协调与扩展模块 | 已完成 | 提交 `ff6fde6`、标签 `render-governance-b3` 与远端分支一致。 |
| 第四批：调度、画质与生产门禁 | 已完成 | 提交 `5a113d6`、标签 `render-governance-b4` 与远端分支一致；标签后独立终验通过。 |
| 独立终验 | 已完成（仓库/本机范围） | 专项命令、完整门禁、三端构建和最新 390×844 生产预览通过。 |
| 手机真机性能验收 | 本轮不执行 / 发布前待验证 | 项目方明确改用本机浏览器验收；不再维护临时 Wi-Fi 地址，也不把桌面结果写成手机通过。 |

状态只能在对应自动化与运行证据通过后更新，不从计划反推完成。

## 第一批证据

- `npm run check`：25 个测试文件、149 项测试通过。
- 覆盖率门槛通过：总体行/语句 89.70%、函数 93.16%、分支 71.44%。
- 1,000 会话与 100 局资源 soak 通过。
- 渲染架构守卫检查 21 个 TypeScript 文件。
- 热路径守卫冻结两个已知分配点；这不表示问题已修复，第三批目标仍为 0。
- 生产依赖安全审计 0 个已知漏洞。
- Web、微信、抖音三端构建和包体预算通过；Web gzip 177,691 bytes。

## 第二批证据

- 完整门禁：25 个测试文件、152 项测试通过。
- 内容全遍历后只创建 1 个 4 MiB 动态菜单纹理；菜单打开总纹理 8,995,072 bytes。
- 菜单关闭后动态纹理归零，总纹理 6,332,928 bytes。
- 390×844 生产预览长按进入 charging，松开后进入 jumping；松开前后纹理创建数均为 16。
- 浏览器控制命令往返 16ms、console 和 Runtime error 为 0；该数据不代表 iPhone 真机性能。
- 详细证据见 [第二批验证](batch-2-verification.md)。

## 第三批证据

- 完整门禁：25 个测试文件、155 项测试通过。
- 架构守卫检查 24 个 Renderer TypeScript 文件及模块依赖矩阵。
- AST 热路径守卫确认受检更新方法中的 Three 对象分配为 0。
- 100 轮特效 soak 中固定两个节点且每轮粒子归零，最终 dispose 节点归零。
- 390×844 生产预览验证固定帧序、charging → jumping、拖尾池更新和零运行错误。
- 详细证据见 [第三批验证](batch-3-verification.md)。

## 第四批证据

- 完整门禁：25 个测试文件、158 项测试通过；覆盖率行/语句 89.91%、函数 93.37%、分支 72.24%。
- 高档 DPR 2、低档 DPR 1.5；低档刷新保持，缓存/动态预算分别为 10/6 MiB。
- 本机生产预览高档松手响应 6.6ms、低档 5.7ms；各自 240 帧窗口长帧 0。
- 起跳前后 CanvasTexture 创建数不增长，Runtime/console 错误 0。
- SaveScheduler 浏览器诊断 queued 2、flushes 2、failed 0；集成测试覆盖首帧后写入及 Hide/Destroy 强制刷新。
- 详细证据见 [第四批验证](batch-4-verification.md)。

## 空中与落地卡顿修复证据

- 修复前本机约 120 Hz 的 3 跳基线：空中位置重复率 45.0%，P99 25.4ms，最大帧 41.3ms。
- Application 新增只读 RenderMotionProjection；固定步长玩法真相、数值、碰撞、WorldState 和回放未改变。
- 平台标签使用预建 SpriteMaterial 池与启动期预上传纹理；HUD 顶栏/状态条改为固定动态纹理，标签、粒子与 HUD GPU 工作按帧错峰。
- 高画质 5 次连续成功落地：P99 15.4ms，空中最大 17.6ms，落地最大 18.0ms，超过 20ms 为 0，空中重复率 0.25%。
- 低画质 5 次连续成功落地：P99 10.3ms，空中/落地最大 10.4ms，超过 20ms 为 0，空中重复率 0%。
- 两档 Runtime、Renderer、console error 为 0；40 次 120 Hz 自动化压力守卫已进入 `npm run check`。
- 最新完整门禁：26 个文件、160 项测试，覆盖率行/语句 90.02%、函数 93.42%、分支 73.00%；生产依赖 0 个已知漏洞。
- 三端构建：Web gzip 183,001 bytes，微信/抖音 `game.js` 各 709,396 bytes，均在预算内。
- 详细证据见[空中与落地卡顿修复验证](jank-remediation.md)。

独立终验与 iPhone 验收边界见 [最终验证](final-verification.md)。
