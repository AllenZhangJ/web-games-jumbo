# Arena V2 武器战斗语言 × KZ 地图后果原型结果 V1

## 目的与边界

本轮把五种最小武器战斗语言接入六段 KZ 灰盒路线，检查同一套地图空间是否能把武器差异转化为可观察的路线后果：

- 直线压制：前摇 8 tick、横向冲量约 6.80；
- 封路：前摇 24 tick、横向冲量约 8.20；
- 延迟重击：前摇 30 tick、横向冲量约 16.65。
- 读招反制：前摇 24 tick、有效窗口 2 tick、横向冲量约 14.20；承诺门槛 12 tick、到期 18 tick。
- 绕后：背后扇区前摇 10 tick、方向阈值 0.65、横向冲量约 12.96。

每个组合都使用当前 `ArenaRuleEngine`、`MovementSystem` 和轻量 `PhysicsWorld`，共运行 6 段 × 5 种语言 × 3 种固定回应 = 90 个探针。固定回应不是真人模型：`hold` 原地承受，`step-out` 在前 10 tick 沿路线前方移动，`jump` 在第 0 tick 起跳。绕后探针额外固定目标背向攻击者，用来隔离方向规则的地图后果。

本原型只存在于开发/测试工具链，不新增生产武器、地图、存档或玩家收藏 ID。封路已经补上“公开标记位置 + 整数 tick 到期”的最小状态，但仍然只是可读预警，不是持续伤害或持续阻挡的危险区；结果不能证明最终区域效果、真人反应时间或正式平衡已经成立。

## 验证入口

```text
packages/arena-v1-experiment/src/arena-v2-kz-language-consequence-prototype.ts
packages/arena-v1-experiment/test/arena-v2-kz-language-consequence-prototype.test.ts
packages/arena-v1-experiment/src/arena-v2-warning-zone-prototype.ts
packages/arena-v1-experiment/test/arena-v2-warning-zone-prototype.test.ts
```

## 固定结果

### 原地等待 `hold`

| 战斗语言 | 命中安全 | 命中击落 | 首次有效 tick | 六段横向冲量合计 | 结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| 直线压制 | 3 | 3 | 8 | 40.78 | 早命中，但地图边缘会把相同冲量放大成击落 |
| 封路 | 3 | 3 | 24 | 49.19 | 预警更长，宽判定在楼梯/窄路更容易改变路线结果 |
| 延迟重击 | 4 | 2 | 30 | 99.90 | 高回报换来最长等待，迷宫/走钢丝仍可能被一次命中击落 |
| 读招反制 | 4 | 2 | 24 | 85.19 | 12 tick 承诺后进入短有效窗口，固定侧移时仍有明显边缘风险 |
| 绕后 | 3 | 3 | 10 | 77.77 | 背后成立依赖目标朝向，方向优势在楼梯/迷宫/走钢丝被放大 |

### 固定回应

| 战斗语言 | 回应 | 命中安全 | 命中击落 | 未命中 | 回应前移动掉落 |
| --- | --- | ---: | ---: | ---: | ---: |
| 直线压制 | `step-out` | 2 | 4 | 0 | 0 |
| 直线压制 | `jump` | 4 | 2 | 0 | 0 |
| 封路 | `step-out` | 2 | 4 | 0 | 0 |
| 封路 | `jump` | 3 | 3 | 0 | 0 |
| 延迟重击 | `step-out` | 0 | 0 | 6 | 3 |
| 延迟重击 | `jump` | 2 | 4 | 0 | 0 |
| 读招反制 | `step-out` | 1 | 3 | 2 | 1 |
| 读招反制 | `jump` | 3 | 3 | 0 | 0 |
| 绕后 | `step-out` | 1 | 5 | 0 | 0 |
| 绕后 | `jump` | 2 | 4 | 0 | 0 |

## 地图后果

六段 `hold` 结果如下；“转移表面”表示命中后的物理结果最终落到相邻表面，不等于玩家一定完成路线转移。

| 路线段 | 直线压制 | 封路 | 延迟重击 | 读招反制 | 绕后 |
| --- | --- | --- | --- | --- | --- |
| 平台起点 | 安全 | 击落 | 安全并转移表面 | 安全并转移表面 | 安全并转移表面 |
| 断层恢复 | 击落 | 击落 | 安全并转移表面 | 安全并转移表面 | 安全并转移表面 |
| 楼梯攀爬 | 击落 | 安全并转移表面 | 安全并转移表面 | 安全并转移表面 | 击落 |
| 迷宫 | 击落 | 击落 | 击落 | 击落 | 击落 |
| 窄路 | 安全并转移表面 | 安全并转移表面 | 安全并转移表面 | 安全并转移表面 | 安全并转移表面 |
| 走钢丝 | 安全 | 安全 | 击落 | 击落 | 击落 |

这说明“武器强不强”不能脱离地图谈：同一把武器的横向控制在起点平台可能只是位移，在迷宫或走钢丝却会直接改变失败概率；同一段窄路又可能把不同前摇的武器都转化为路线转移，而不是单纯伤害差异。

## 封路最小标记状态

封路候选从动作开始后创建一个公开标记，使用同一整数 tick 时间轴：

- `telegraph`：第 0–23 tick，玩家能看到标记位置，但不受区域判定影响；
- `active`：第 24–26 tick，标记处于 3 tick 的有效阶段；
- `expired`：第 27 tick 起，标记不再判定玩家是否处于区域内。

六段 KZ 探针中的封路候选均记录到 `startsAtTick=24`、`expiresAtTickExclusive=27`，并在命中或提前掉落前至少观察到有效阶段。当前点内判定只返回公开状态，不生成伤害、击退或新的输入要求，因此不会绕过 `ArenaRuleEngine` 的动作命中链路。

## 命中反馈因果合同

本轮在每个 KZ 探针结果上增加只读 `feedback` 语义，避免把所有失败都显示成同一种“被击落”：

| feedback kind | 玩家应理解的原因 | 典型表现 |
|---|---|---|
| `hit-confirm` | 命中成立，但目标仍有支撑面 | 命中特效 + 位移提示 |
| `hit-surface-transfer` | 命中改变了最终落点或支撑面 | 命中特效 + 落点/路线转移提示 |
| `hit-ring-out` | 命中把目标推出安全支撑面 | 强命中特效 + 击落提示 |
| `attack-evaded` | 目标在有效判定前离开攻击线 | 空放反馈 +“已避开攻击线” |
| `movement-fall` | 玩家在命中前先因路线失误掉落 | 路线失误反馈，不播放武器命中特效 |

这五种语义由同一份命中 tick、最终支撑面、掉落状态和回应结果推导。现在已通过 `projectArenaV2WeaponFeedbackPresentationEvent` 映射为统一的 `WeaponFeedbackPresented` 表现事件，并由现有 `PresentationEventWindow` 负责稳定去重；Three 灰盒已经消费这些 Cue，分别显示冲击或警告效果，并接入既有镜头、震动和音频触发入口。表现层仍不重新判断原因；最终声音/特效资产、设备可读性和真人反馈尚未完成，边界见[ADR-069](../decisions/069-arena-v2-hit-feedback-causal-contract.md)、[ADR-071](../decisions/071-arena-v2-feedback-presentation-event-mapping.md)与[ADR-072](../decisions/072-arena-v2-feedback-presentation-three-consumption.md)。

## 研究判断

### 已经得到的证据

- 五种战斗语言确实产生了不同的地图后果，不是只换名称或外观；
- `startup`、命中冲量、地形宽度和相邻支撑面共同决定“命中后发生什么”；
- 读招反制的 2 tick 有效窗口与 14.20 横向冲量，使其在固定侧移下比直线压制更容易形成边缘击落；
- 绕后不是宽正面攻击：目标背向攻击者时可以命中，且在楼梯、迷宫和走钢丝上形成不同于读招反制的掉落分布；
- 延迟重击的 30 tick 预警在固定侧移脚本下全部规避，说明高击退必须用明确的预警和位置承诺交换，不应只给更高收益；
- KZ 地图可作为武器学习的放大器：玩家在宽平台先学攻击语言，再在断层、迷宫、窄路和走钢丝里学习位置后果。

### 仍然不能关闭的问题

- `step-out` 和 `jump` 是固定脚本，不能代替真人的视野、输入延迟和路线判断；
- 当前封路只有公开标记和到期状态，没有持续伤害、阻挡或区域击退，不能宣称已经验证了“封路”完整功能；
- 当前结果没有敌人追击、复活重新进入路线或多人拥挤，不能直接冻结竞速/生存数值；
- 绕后目标朝向是固定脚本，尚未验证目标主动转身和多人视线遮挡。
- 命中反馈的声音、特效、镜头和伤害解释率仍未进行设备/真人验证。
- 五种反馈语义已经接入正式 Presentation 事件，并被 Three 灰盒冲击/警告效果、镜头、震动和现有音频入口消费；最终资产绑定和设备表现验证仍未完成。

## 收敛决定

下一批优先保留五种语言的差异，不增加新的按键：

1. 直线压制作为低前摇、低击退的基础学习样本；
2. 封路先保留“公开标记位置 + 到期”的最小权威状态，再决定是否需要持续区域影响；
3. 延迟重击保留最长预警和最高击退，并把可规避性作为硬验收条件；
4. 读招反制必须继续保留短有效窗口，绕后必须继续使用目标朝向判定；
5. 正式武器候选必须同时提交动作前摇、有效窗口、方向条件、命中冲量、地图段落结果和玩家反制说明，不能只提交总评分。

## 关联文档

- [武器战斗语言最小原型结果 V1](arena-v2-weapon-language-prototype-results-v1.md)
- [CS1.6 KZ 跳跃地图研究 V1](arena-v2-cs16-kz-map-study-v1.md)
- [ADR-057：武器战斗语言必须通过地图后果验证](../decisions/057-arena-v2-weapon-language-map-consequence-boundary.md)
- [ADR-069：命中反馈必须保留失败原因的因果区分](../decisions/069-arena-v2-hit-feedback-causal-contract.md)
- [ADR-071：武器反馈先映射为表现事件，再绑定声音与特效](../decisions/071-arena-v2-feedback-presentation-event-mapping.md)
- [ADR-072：正式武器反馈事件接入 Three 灰盒表现](../decisions/072-arena-v2-feedback-presentation-three-consumption.md)
