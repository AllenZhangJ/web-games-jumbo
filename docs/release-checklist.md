# 发布清单

## 合并前

- [ ] `npm ci` 使用锁文件成功安装，`npm audit --omit=dev --audit-level=high` 无高危生产依赖。
- [ ] `npm run check` 全部通过：lint、strict TypeScript、单测、覆盖率、零旧 JS、资产/许可证审计和三端构建。
- [ ] P0–P8 行为基线、左右按钮投影映射和镜头过渡没有变化。
- [ ] 存档 v1/v2/v3 迁移、损坏存档隔离、回放恢复和清除路径通过。
- [ ] 5 个正式玩法、5 个正式任务、10 个正式角色的目录、兼容组合和资源生命周期测试继续通过。

## 设备验收

- [ ] 390×844 Web 主流程完成左右各一次长按跳跃，页面无选中/复制菜单，控制台零错误。
- [ ] Web `pagehide/pageshow`、尺寸变化、WebGL 上下文丢失/恢复已验证。
- [ ] 微信与抖音开发者工具载入对应 `dist`，至少各完成一次启动、跳跃、隐藏/显示和存档恢复。
- [ ] 至少一台真实手机通过同一 Wi-Fi 地址完成启动、左右跳跃、刷新恢复、声音/振动独立开关验收。

## 发布与回滚

- [ ] `CHANGELOG.md` 已从 `Unreleased` 整理到版本号，三端产物包含许可证与 `THIRD_PARTY_NOTICES.md`。
- [ ] 主分支保护要求 GitHub Actions `quality` 成功、禁止直接推送、至少 1 名代码所有者审批。
- [ ] 创建签名或带注释版本标签，并记录提交、构建环境、包体大小和设备证据。
- [ ] 保留上一稳定版本产物；回滚只切换完整版本，不跨版本复用不可迁移的存档结构。
